// Re-export built Vue bundle for SPFx embedding.
// After `npm run build` in /frontend, copy dist assets here or use build script.
export { mountMechanizationApp } from '../../../frontend/src/main';
